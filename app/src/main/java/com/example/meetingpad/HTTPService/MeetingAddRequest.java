package com.example.meetingpad.HTTPService;

//增加一个会议所需要用到的请求
public class MeetingAddRequest {
    // 根据选择的日期，显示当日剩余可用会议室时间轴分布

    //给出RoomRestrict，返回可用的会议室集合List<Room>

    // 一步提交会议和会程并安排已经选择的参会人员
    // 参数：String mTitle, String mInfo, int mSize, int mSpan, String pId_FQ, String rId,
    //			String startDate, String startTime, String endTime, String attendList


}
